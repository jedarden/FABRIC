# npm Audit Vulnerability Fixes (bf-563v5)

Date: 2026-07-24

## Vulnerabilities Resolved

Ran `npm audit fix` which successfully resolved all 5 vulnerabilities (1 low, 2 moderate, 2 high):

### HIGH Severity
1. **path-to-regexp**: 8.3.0 → 8.4.2
   - Fixed: DoS via sequential optional groups (GHSA-j3q9-mxjg-w52f)
   - Fixed: Regular Expression DoS via multiple wildcards (GHSA-27v5-c462-wpq7)
   - Impact: Transitive dependency via express

2. **ws**: 8.19.0 → 8.21.1
   - Fixed: Uninitialized memory disclosure (GHSA-58qx-3vcg-4xpx)
   - Fixed: Memory exhaustion DoS from tiny fragments (GHSA-96hv-2xvq-fx4p)
   - Impact: Direct dependency for WebSocket event streaming

### MODERATE Severity
3. **protobufjs**: 7.6.2 → 7.6.5
   - Fixed: Schema-derived names can shadow runtime properties (GHSA-f38q-mgvj-vph7)
   - Impact: Transitive via @grpc/proto-loader for OTLP receiver

4. **qs**: 6.14.1 → 6.15.2
   - Fixed: DoS via qs.stringify on null/undefined in arrays (GHSA-q8mj-m7cp-5q26)
   - Impact: Transitive dependency

### LOW Severity
5. **body-parser**: Updated to latest secure version
   - Fixed: DoS when invalid limit value silently disables size enforcement

## Verification

✅ `npm audit --omit=dev` reports **0 vulnerabilities**
✅ `npm run build:web` completes successfully
✅ Core CLI functionality verified (`fabric --version` returns 0.1.0)
✅ No breaking changes from dependency updates

## Pre-existing Issues (Not Related to This Fix)

The build has pre-existing TypeScript compilation errors in test files (`src/tui/components/SessionReplay.test.ts`) and `src/cli.ts` that existed before these security fixes. These are unrelated to the npm audit fixes.

## Command Run

```bash
npm audit fix
```

Result: added 2 packages, removed 1 package, changed 52 packages.
